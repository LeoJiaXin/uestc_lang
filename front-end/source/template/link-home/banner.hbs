{{#each banners}}
<img src="{{img}}" alt="">
{{/each}}
<div class="wrapper-banner-switch">
    {{#each banners}}
    <div class="banner-switch{{#if @first}} select{{/if}}"></div>
    {{/each}}
    <div class="clear"></div>
</div>
